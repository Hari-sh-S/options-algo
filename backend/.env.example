# Dhan API credentials (passed from frontend per-request, but can set defaults here)
DHAN_CLIENT_ID=
DHAN_ACCESS_TOKEN=

# Server
HOST=0.0.0.0
PORT=8000

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
